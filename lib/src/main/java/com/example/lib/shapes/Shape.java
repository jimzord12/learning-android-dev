package com.example.lib.shapes;

public interface Shape {
    double calculateParameter(double radius);
    double calculateParameter();
    double calculateVolume();
    double calculateVolume(double radius);
}
